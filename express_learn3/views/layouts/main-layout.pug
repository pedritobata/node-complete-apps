<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        meta(http-equiv="X-UA-Compatible", content="ie=edge")
        title #{pageTitle}
        link(rel="stylesheet", href="/css/main.css")
        block styles
    body
        header.main-header
            nav.main-header__navç
                ul.main-header__item-list
                    li.main-header__item
                        a(href='/',
                        class=( path === '/' ? 'active' : '')) Shop
                    li.main-header__item
                        // puedo poner codigo js entre los parentesis
                        a(href='/admin/add-product', 
                        class=( path === '/admin/add-product' ? 'active' : '')) Add Product
        //con la palabra block indicamos a pug que aqui irá el contenido dinamico
        //y que se invocará con el nombre que le estamos poniendo, en este caso es content
        block content